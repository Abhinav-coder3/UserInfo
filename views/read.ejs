<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    
     
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 p-10 text-white">
        <div class="mb-10">
            <a href="/" class="text-blue-500">Home</a>
        </div>
        <h3 class="text-3xl tracking-tighter mb-3">All Users</h3>
        
     <div class="users flex gap-3 flex-wrap">

        <% if(user.length > 0) { %>
                   <% user.forEach(function(users){ %>
        <div class="user w-72 p-4  bg-zinc-800 rounded-lg">
            <div class="w-full h-52 bg-zinc-600 rounded-lg overflow-hidden">
                <img class="w-full h-full object-cover"  src="<%= users.image %>" alt="">
            </div>
            <h3 class="text-white text-xl tracking-tighter"><%= users.name %></h3>
            <h5 class="text-zinc-500"><%= users.email %></h5>
            <div class="flex gap-3 mt-10">
                <a href="/edit/<%= users._id %>" class="text-yellow-500">edit user</a>
                <a href="/delete/<%= users._id %>"class="text-red-600" >delete user</a>
            </div>
            
      </div>
           <% }) %>
           
             <% } else { %>
              <h3>No Users yet</h3>
        <% } %>

     </div>
    </div>

</body>
</html>